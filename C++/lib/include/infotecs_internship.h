#pragma once

#include <string>
#include <algorithm>

#ifndef infotecs_internship_h
#define infotecs_internship_h

void sortKB(std::string& str);
std::string sum(const std::string & str);
bool isSize32(const std::string& num_str);

#endif